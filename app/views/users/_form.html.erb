<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <font size="4"><li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <font size="4"><%= f.label :userid ,"ユーザーID" %></font><br>
    <%= f.text_field :userid %>
  </div>
  <div class="field">
    <font size="4"><%= f.label :username ,"ユーザー名" %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <font size="4"><%= f.label :password ,"パスワード" %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
